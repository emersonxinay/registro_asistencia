@model SeleccionarRamoViewModel

<div class="workflow-container">
    <!-- Header del Workflow -->
    <div class="workflow-header">
        <div class="workflow-breadcrumb">
            <span class="step completed"><i class="fas fa-check"></i> 1. Curso Elegido</span>
            <span class="step active"><i class="fas fa-bullseye"></i> 2. Elegir Materia</span>
            <span class="step"><i class="fas fa-cog"></i> 3. Configurar Clase</span>
            <span class="step"><i class="fas fa-flag-checkered"></i> 4. ¡Clase Lista!</span>
        </div>
        <h1><i class="fas fa-bullseye"></i> Paso 2: Elige la Materia/Asignatura</h1>
        <p>Selecciona la <strong>materia específica</strong> del curso <strong>@Model.Curso.Nombre</strong> donde vas a crear tu nueva clase</p>
        <div class="workflow-explanation">
            <div class="explanation-card">
                <div class="explanation-icon"><i class="fas fa-lightbulb"></i></div>
                <div class="explanation-text">
                    <strong>¿Qué viene después?</strong> Una vez que elijas la materia, podrás configurar los detalles de tu clase (fecha, hora, duración) y generar el código QR para que los estudiantes registren su asistencia.
                </div>
            </div>
        </div>
    </div>

    <!-- Información del Curso Seleccionado -->
    <div class="selected-course-info">
        <div class="course-summary">
            <div class="course-icon"><i class="fas fa-book"></i></div>
            <div class="course-details">
                <h3>@Model.Curso.Nombre</h3>
                <p class="course-code">@Model.Curso.Codigo</p>
                @if (!string.IsNullOrEmpty(Model.Curso.Descripcion))
                {
                    <p class="course-description">@Model.Curso.Descripcion</p>
                }
            </div>
        </div>
    </div>

    <!-- Progreso Visual -->
    <div class="progress-indicator">
        <div class="progress-steps">
            <div class="progress-step completed">
                <div class="step-circle">✓</div>
                <div class="step-label">Curso Seleccionado</div>
            </div>
            <div class="progress-arrow">→</div>
            <div class="progress-step active">
                <div class="step-circle">2</div>
                <div class="step-label">Elige Materia</div>
            </div>
            <div class="progress-arrow">→</div>
            <div class="progress-step pending">
                <div class="step-circle">3</div>
                <div class="step-label">Configurar Clase</div>
            </div>
        </div>
    </div>

    <!-- Lista de Ramos -->
    <div class="ramos-selection">
        @if (!Model.Ramos.Any())
        {
            <div class="no-ramos">
                <div class="empty-state">
                    <div class="empty-icon"><i class="fas fa-book-open"></i></div>
                    <h3><i class="fas fa-exclamation-triangle"></i> No puedes crear clases aún</h3>
                    <p><strong>Este curso no tiene materias/asignaturas configuradas.</strong></p>
                    <p>Para crear una clase, primero necesitas tener al menos una materia o asignatura en este curso. Las clases se crean dentro de materias específicas.</p>
                    <div class="empty-actions">
                        <a href="/docente/cursos/@Model.Curso.Id" class="btn-primary">
                            <i class="fas fa-book"></i> Gestionar Materias del Curso
                        </a>
                    </div>
                </div>
            </div>
        }
        else
        {
            <div class="ramos-grid">
                @foreach (var ramo in Model.Ramos)
                {
                    <div class="ramo-card" onclick="selectRamo(@ramo.Id, '@ramo.Nombre', @Model.Curso.Id)">
                        <div class="ramo-header">
                            <div class="ramo-icon"><i class="fas fa-book"></i></div>
                            <div class="ramo-title">
                                <h3>@ramo.Nombre</h3>
                                <span class="ramo-code">@ramo.Codigo</span>
                            </div>
                        </div>
                        
                        @if (!string.IsNullOrEmpty(ramo.Descripcion))
                        {
                            <div class="ramo-description">
                                <p>@ramo.Descripcion</p>
                            </div>
                        }
                        
                        <div class="ramo-stats">
                            <div class="stat-item">
                                <span class="stat-icon"><i class="fas fa-clock"></i></span>
                                <span class="stat-text">@ramo.HorasSemanales hrs/semana</span>
                            </div>
                            <div class="stat-item">
                                <span class="stat-icon"><i class="fas fa-calendar"></i></span>
                                <span class="stat-text">@ramo.TotalClases clases creadas</span>
                            </div>
                        </div>
                        
                        <div class="ramo-actions">
                            <button class="btn-select" onclick="selectRamo(@ramo.Id, '@ramo.Nombre', @Model.Curso.Id)">
                                <span><i class="fas fa-bullseye"></i> Crear Clase en @ramo.Nombre</span>
                                <span class="arrow">→</span>
                            </button>
                        </div>
                    </div>
                }
            </div>
        }
    </div>

    <!-- Acciones del Workflow -->
    <div class="workflow-actions">
        <a href="/workflow/cursos" class="btn-secondary">
            ← Cambiar Curso
        </a>
        
        <div class="action-right">
            @if (Model.Curso.EsPropietario)
            {
                <a href="/workflow/curso/@Model.Curso.Id/asistencias" class="btn-outline">
                    <i class="fas fa-chart-bar"></i> Ver Asistencias del Curso
                </a>
                <a href="/docente/cursos/@Model.Curso.Id/ramos" class="btn-outline">
                    <i class="fas fa-plus"></i> Crear Nueva Materia
                </a>
            }
        </div>
    </div>
</div>

<script>
function selectRamo(ramoId, ramoNombre, cursoId) {
    // Mostrar loading
    const card = event.currentTarget;
    const originalContent = card.innerHTML;
    card.innerHTML = `
        <div class="loading-state">
            <div class="spinner"></div>
            <p><i class="fas fa-bullseye"></i> Configurando nueva clase...</p>
            <small>Te llevaremos al formulario de configuración</small>
        </div>
    `;
    
    // Navegar al siguiente paso
    window.location.href = `/workflow/curso/${cursoId}/ramo/${ramoId}/crear-clase`;
}

// Animaciones de entrada
document.addEventListener('DOMContentLoaded', function() {
    const cards = document.querySelectorAll('.ramo-card');
    cards.forEach((card, index) => {
        card.style.animationDelay = `${index * 0.1}s`;
        card.classList.add('fade-in');
    });
});
</script>

<style>
.workflow-container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 20px;
}

.workflow-header {
    text-align: center;
    margin-bottom: 40px;
}

.workflow-breadcrumb {
    display: flex;
    justify-content: center;
    gap: 20px;
    margin-bottom: 30px;
    flex-wrap: wrap;
}

.workflow-breadcrumb .step {
    background: #f8f9fa;
    color: #6c757d;
    padding: 10px 20px;
    border-radius: 25px;
    font-size: 0.9em;
    font-weight: 500;
    position: relative;
}

.workflow-breadcrumb .step.completed {
    background: #27ae60;
    color: white;
    box-shadow: 0 2px 8px rgba(39, 174, 96, 0.3);
}

.workflow-breadcrumb .step.active {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    color: white;
    box-shadow: 0 3px 10px rgba(102, 126, 234, 0.3);
}

.workflow-header h1 {
    color: var(--text-primary);
    margin-bottom: 10px;
    font-size: 2.5em;
}

.workflow-header p {
    color: var(--text-secondary);
    font-size: 1.2em;
    margin: 0 0 20px 0;
}

.workflow-explanation {
    margin-bottom: 20px;
}

.explanation-card {
    background: var(--bg-secondary);
    border: 2px solid var(--success);
    border-radius: 12px;
    padding: 20px;
    display: flex;
    align-items: center;
    gap: 15px;
    max-width: 800px;
    margin: 0 auto;
    box-shadow: var(--shadow);
}

.explanation-icon {
    font-size: 2em;
    background: var(--success);
    color: var(--text-on-primary);
    width: 50px;
    height: 50px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
}

.explanation-text {
    color: var(--text-primary);
    font-size: 1em;
    line-height: 1.5;
}

.explanation-text strong {
    color: var(--success);
}

.progress-indicator {
    background: var(--bg-primary);
    border-radius: 15px;
    padding: 25px;
    margin-bottom: 30px;
    box-shadow: var(--shadow);
    border: 1px solid var(--border);
}

.progress-steps {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 20px;
}

.progress-step {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 8px;
}

.step-circle {
    width: 45px;
    height: 45px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 700;
    font-size: 1.1em;
    transition: all 0.3s ease;
}

.progress-step.completed .step-circle {
    background: var(--success);
    color: var(--text-on-primary);
    box-shadow: 0 4px 12px rgba(39, 174, 96, 0.3);
}

.progress-step.active .step-circle {
    background: var(--primary-gradient);
    color: var(--text-on-primary);
    box-shadow: var(--shadow-lg);
    transform: scale(1.1);
}

.progress-step.pending .step-circle {
    background: var(--bg-secondary);
    color: var(--text-muted);
    border: 2px solid var(--border);
}

.step-label {
    font-size: 0.85em;
    font-weight: 600;
    text-align: center;
    min-width: 80px;
}

.progress-step.completed .step-label {
    color: var(--success);
}

.progress-step.active .step-label {
    color: var(--primary);
}

.progress-step.pending .step-label {
    color: var(--text-muted);
}

.progress-arrow {
    font-size: 1.5em;
    color: var(--primary);
    font-weight: bold;
}

.selected-course-info {
    background: var(--bg-secondary);
    border-radius: 15px;
    padding: 25px;
    margin-bottom: 40px;
    border-left: 5px solid var(--primary);
    border: 1px solid var(--border);
}

.course-summary {
    display: flex;
    align-items: center;
    gap: 20px;
}

.course-icon {
    font-size: 3em;
    background: var(--bg-primary);
    border-radius: 50%;
    width: 80px;
    height: 80px;
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow: var(--shadow);
}

.course-details h3 {
    margin: 0 0 8px 0;
    color: var(--text-primary);
    font-size: 1.8em;
}

.course-code {
    background: var(--primary);
    color: var(--text-on-primary);
    padding: 4px 12px;
    border-radius: 6px;
    font-size: 0.9em;
    font-weight: 600;
    display: inline-block;
    margin-bottom: 10px;
}

.course-description {
    color: var(--text-secondary);
    font-size: 1.1em;
    margin: 0;
    line-height: 1.5;
}

.ramos-selection {
    margin-bottom: 40px;
}

.no-ramos {
    display: flex;
    justify-content: center;
    align-items: center;
    min-height: 400px;
}

.empty-state {
    text-align: center;
    padding: 40px;
    background: var(--bg-primary);
    border-radius: 15px;
    box-shadow: var(--shadow);
    border: 1px solid var(--border);
}

.empty-icon {
    font-size: 4em;
    margin-bottom: 20px;
    opacity: 0.6;
}

.empty-state h3 {
    color: var(--text-primary);
    margin-bottom: 15px;
    font-size: 1.6em;
}

.empty-state p {
    color: var(--text-secondary);
    margin-bottom: 25px;
    font-size: 1.1em;
    line-height: 1.6;
}

.empty-actions {
    display: flex;
    justify-content: center;
    gap: 15px;
}

.ramos-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(380px, 1fr));
    gap: 25px;
}

.ramo-card {
    background: var(--bg-primary);
    border: 2px solid var(--border);
    border-radius: 15px;
    padding: 25px;
    cursor: pointer;
    transition: var(--transition);
    position: relative;
    overflow: hidden;
}

.ramo-card:hover {
    border-color: var(--primary);
    transform: translateY(-5px);
    box-shadow: var(--shadow-lg);
}

.ramo-card.fade-in {
    animation: fadeInUp 0.6s ease forwards;
    opacity: 0;
    transform: translateY(20px);
}

@@keyframes fadeInUp {
    to {
        opacity: 1;
        transform: translateY(0);
    }
}

.ramo-header {
    display: flex;
    align-items: center;
    gap: 15px;
    margin-bottom: 20px;
}

.ramo-icon {
    background: var(--primary-gradient);
    color: var(--text-on-primary);
    font-size: 1.8em;
    width: 50px;
    height: 50px;
    border-radius: 12px;
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow: var(--shadow);
}

.ramo-title {
    flex: 1;
}

.ramo-title h3 {
    margin: 0 0 8px 0;
    color: var(--text-primary);
    font-size: 1.4em;
    line-height: 1.3;
}

.ramo-code {
    background: var(--bg-secondary);
    color: var(--text-primary);
    padding: 4px 10px;
    border-radius: 6px;
    font-size: 0.85em;
    font-weight: 600;
}

.ramo-description {
    margin-bottom: 20px;
    padding: 15px;
    background: var(--bg-secondary);
    border-radius: 8px;
    border-left: 4px solid var(--primary);
}

.ramo-description p {
    margin: 0;
    color: var(--text-secondary);
    font-size: 0.95em;
    line-height: 1.6;
}

.ramo-stats {
    margin-bottom: 25px;
    display: flex;
    justify-content: space-between;
    gap: 20px;
}

.stat-item {
    display: flex;
    align-items: center;
    gap: 8px;
    flex: 1;
}

.stat-icon {
    font-size: 1.1em;
    color: var(--primary);
}

.stat-text {
    color: var(--text-secondary);
    font-size: 0.9em;
}

.ramo-actions {
    margin-top: auto;
}

.btn-select {
    width: 100%;
    background: var(--primary-gradient);
    color: var(--text-on-primary);
    border: none;
    padding: 15px 25px;
    border-radius: 10px;
    font-size: 1em;
    font-weight: 600;
    cursor: pointer;
    transition: var(--transition);
    display: flex;
    align-items: center;
    justify-content: space-between;
    box-shadow: var(--shadow);
}

.btn-select:hover {
    transform: translateY(-2px);
    box-shadow: var(--shadow-lg);
}

.btn-select .arrow {
    font-size: 1.2em;
    transition: transform 0.3s ease;
}

.btn-select:hover .arrow {
    transform: translateX(5px);
}

.loading-state {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    padding: 40px;
    background: var(--primary-gradient);
    color: var(--text-on-primary);
    border-radius: 15px;
}

.loading-state p {
    margin: 10px 0 5px 0;
    font-weight: 600;
    font-size: 1.1em;
}

.loading-state small {
    opacity: 0.9;
    font-size: 0.9em;
    margin: 0;
}

.spinner {
    width: 40px;
    height: 40px;
    border: 4px solid #f3f3f3;
    border-top: 4px solid #3498db;
    border-radius: 50%;
    animation: spin 1s linear infinite;
    margin-bottom: 15px;
}

@@keyframes spin {
    0% { transform: rotate(0deg); }
    100% { transform: rotate(360deg); }
}

.workflow-actions {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding-top: 30px;
    border-top: 1px solid var(--border);
}

.action-right {
    display: flex;
    gap: 15px;
}

.btn-primary, .btn-secondary, .btn-outline {
    padding: 12px 24px;
    border-radius: 8px;
    text-decoration: none;
    font-weight: 600;
    transition: var(--transition);
    display: inline-flex;
    align-items: center;
    gap: 8px;
}

.btn-primary {
    background: var(--primary-gradient);
    color: var(--text-on-primary);
    border: none;
    box-shadow: var(--shadow);
}

.btn-primary:hover {
    transform: translateY(-2px);
    box-shadow: var(--shadow-lg);
}

.btn-secondary {
    background: var(--text-muted);
    color: var(--text-on-primary);
    border: none;
}

.btn-secondary:hover {
    background: var(--text-secondary);
    transform: translateY(-1px);
}

.btn-outline {
    background: transparent;
    color: var(--primary);
    border: 2px solid var(--primary);
}

.btn-outline:hover {
    background: var(--primary);
    color: var(--text-on-primary);
}

/* Responsive */
@@media (max-width: 768px) {
    .workflow-breadcrumb {
        gap: 10px;
    }

    .workflow-breadcrumb .step {
        font-size: 0.8em;
        padding: 8px 15px;
    }

    .progress-steps {
        flex-direction: column;
        gap: 10px;
    }

    .progress-arrow {
        transform: rotate(90deg);
        font-size: 1.2em;
    }

    .explanation-card {
        flex-direction: column;
        text-align: center;
        gap: 10px;
    }

    .course-summary {
        flex-direction: column;
        text-align: center;
        gap: 15px;
    }

    .ramos-grid {
        grid-template-columns: 1fr;
        gap: 20px;
    }

    .ramo-stats {
        flex-direction: column;
        gap: 10px;
    }

    .workflow-actions {
        flex-direction: column;
        gap: 15px;
    }

    .action-right {
        justify-content: center;
    }
}

@@media (max-width: 480px) {
    .workflow-container {
        padding: 15px;
    }
    
    .workflow-header h1 {
        font-size: 2em;
    }
    
    .selected-course-info {
        padding: 20px;
    }
    
    .ramo-card {
        padding: 20px;
    }
    
    .course-icon {
        width: 60px;
        height: 60px;
        font-size: 2.5em;
    }
}
</style>