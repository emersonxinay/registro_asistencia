@{
    ViewData["Title"] = "Registrar Asistencia";
    var returnUrl = $"{Context.Request.Path}{Context.Request.QueryString}";
}

@section Styles {
    <style>
        /* DISEÑO RESPONSIVE - 100% EM/REM - SIN FIXED - DARK MODE COMPATIBLE */

        /* Variables adicionales para Scan (compatible con theme-system.css) */
        :root {
            --gradient-primary: linear-gradient(135deg,
                hsl(199, 89%, 48%),
                hsl(187, 95%, 43%));
            --accent-color: hsl(173, 80%, 40%);
            --focus-ring: hsla(173, 80%, 40%, 0.2);
        }

        [data-theme="dark"] {
            --gradient-primary: linear-gradient(135deg,
                hsl(199, 89%, 58%),
                hsl(187, 95%, 53%));
            --accent-color: hsl(173, 80%, 50%);
            --focus-ring: hsla(173, 80%, 50%, 0.3);
        }

        /* Page Container */
        .scan-page {
            width: 100%;
            max-width: 50rem;
            margin: 0 auto;
            padding: 1rem;
            padding-bottom: 2rem;
        }

        @@media(min-width: 48em) {
            .scan-page {
                padding: 1.5rem;
                padding-bottom: 2.5rem;
            }
        }

        @@media(min-width: 64em) {
            .scan-page {
                padding: 2rem;
                padding-bottom: 3rem;
            }
        }

        /* Class Info Badge */
        .class-info-badge {
            background: linear-gradient(135deg, rgba(102, 126, 234, 0.12), rgba(118, 75, 162, 0.08));
            border: 0.125rem solid var(--accent-color);
            border-radius: 0.75rem;
            padding: 0.75rem 1rem;
            margin-bottom: 1rem;
            display: flex;
            align-items: center;
            gap: 0.75rem;
            box-shadow: 0 0.125rem 0.5rem rgba(102, 126, 234, 0.15);
        }

        @@media(min-width: 48em) {
            .class-info-badge {
                padding: 1rem 1.25rem;
                margin-bottom: 1.25rem;
            }
        }

        .class-info-icon {
            width: 2.25rem;
            height: 2.25rem;
            background: var(--gradient-primary);
            border-radius: 0.625rem;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 1.125rem;
            flex-shrink: 0;
        }

        @@media(min-width: 48em) {
            .class-info-icon {
                width: 2.5rem;
                height: 2.5rem;
                font-size: 1.25rem;
            }
        }

        .class-info-text {
            flex: 1;
            min-width: 0;
        }

        .class-info-title {
            font-size: 0.9375rem;
            font-weight: 700;
            color: var(--text-primary);
            margin-bottom: 0.25rem;
            line-height: 1.3;
            display: -webkit-box;
            -webkit-line-clamp: 2;
            -webkit-box-orient: vertical;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        @@media(min-width: 40em) {
            .class-info-title {
                font-size: 1rem;
                -webkit-line-clamp: 1;
                white-space: nowrap;
            }
        }

        .class-info-name {
            font-size: 0.75rem;
            color: var(--text-secondary);
            font-weight: 500;
        }

        @@media(min-width: 40em) {
            .class-info-name {
                font-size: 0.8125rem;
            }
        }

        .class-id-badge {
            background: var(--accent-color);
            color: white;
            padding: 0.375rem 0.625rem;
            border-radius: 0.625rem;
            font-size: 0.6875rem;
            font-weight: 700;
            flex-shrink: 0;
            white-space: nowrap;
            align-self: flex-start;
            margin-top: 0.125rem;
        }

        @@media(min-width: 40em) {
            .class-id-badge {
                padding: 0.5rem 0.875rem;
                border-radius: 0.875rem;
                font-size: 0.75rem;
                align-self: center;
                margin-top: 0;
            }
        }

        /* Cards */
        .card {
            background: var(--bg-secondary);
            border: 0.0625rem solid var(--border-color);
            border-radius: 0.875rem;
            padding: 1rem;
            margin-bottom: 1rem;
            box-shadow: var(--shadow-sm);
        }

        @@media(min-width: 48em) {
            .card {
                padding: 1.25rem;
                margin-bottom: 1.25rem;
            }
        }

        .card-header {
            display: flex;
            align-items: center;
            gap: 0.625rem;
            margin-bottom: 0.875rem;
            padding-bottom: 0.75rem;
            border-bottom: 0.0625rem solid var(--border-color);
        }

        @@media(min-width: 40em) {
            .card-header {
                gap: 0.75rem;
                margin-bottom: 1rem;
                padding-bottom: 0.875rem;
            }
        }

        .card-icon {
            width: 2.5rem;
            height: 2.5rem;
            border-radius: 0.75rem;
            background: var(--gradient-primary);
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 1.125rem;
            flex-shrink: 0;
        }

        @@media(min-width: 40em) {
            .card-icon {
                width: 2.75rem;
                height: 2.75rem;
                border-radius: 0.875rem;
                font-size: 1.25rem;
            }
        }

        .card-title {
            flex: 1;
            font-size: 0.9375rem;
            font-weight: 600;
            color: var(--text-primary);
        }

        @@media(min-width: 40em) {
            .card-title {
                font-size: 1rem;
            }
        }

        .card-desc {
            font-size: 0.8125rem;
            color: var(--text-secondary);
            margin-bottom: 0.875rem;
            line-height: 1.4;
        }

        @@media(min-width: 40em) {
            .card-desc {
                font-size: 0.875rem;
                margin-bottom: 1rem;
            }
        }

        /* Instructions Card */
        .instructions-card {
            background: var(--bg-secondary);
            border: 0.0625rem solid var(--border-color);
            border-radius: 0.875rem;
            padding: 1rem;
            margin-bottom: 1rem;
        }

        @@media(min-width: 48em) {
            .instructions-card {
                padding: 1.25rem;
                margin-bottom: 1.25rem;
            }
        }

        .instructions-header {
            display: flex;
            align-items: center;
            gap: 0.625rem;
            font-size: 0.9375rem;
            font-weight: 700;
            color: var(--text-primary);
            margin-bottom: 0.875rem;
            padding-bottom: 0.75rem;
            border-bottom: 0.125rem solid var(--border-color);
        }

        @@media(min-width: 40em) {
            .instructions-header {
                gap: 0.75rem;
                font-size: 1rem;
                margin-bottom: 1rem;
                padding-bottom: 0.875rem;
            }
        }

        .instructions-header i {
            color: var(--accent-color);
            font-size: 1.25rem;
        }

        @@media(min-width: 40em) {
            .instructions-header i {
                font-size: 1.375rem;
            }
        }

        .instructions-list {
            display: flex;
            flex-direction: column;
            gap: 0.75rem;
        }

        @@media(min-width: 40em) {
            .instructions-list {
                gap: 0.875rem;
            }
        }

        .instruction-item {
            display: flex;
            align-items: flex-start;
            gap: 0.75rem;
            padding: 0.75rem;
            background: var(--bg-primary);
            border-radius: 0.75rem;
            border: 0.0625rem solid var(--border-color);
        }

        @@media(min-width: 40em) {
            .instruction-item {
                gap: 0.875rem;
                padding: 0.875rem;
                border-radius: 0.875rem;
            }
        }

        .instruction-number {
            width: 2rem;
            height: 2rem;
            background: var(--gradient-primary);
            color: white;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 700;
            font-size: 0.875rem;
            flex-shrink: 0;
        }

        .instruction-number i {
            font-size: 0.9375rem;
        }

        @@media(min-width: 40em) {
            .instruction-number {
                width: 2.25rem;
                height: 2.25rem;
                font-size: 0.9375rem;
            }

            .instruction-number i {
                font-size: 1rem;
            }
        }

        .instruction-content {
            flex: 1;
            min-width: 0;
        }

        .instruction-title {
            font-size: 0.875rem;
            font-weight: 600;
            color: var(--text-primary);
            margin-bottom: 0.25rem;
        }

        @@media(min-width: 40em) {
            .instruction-title {
                font-size: 0.9375rem;
                margin-bottom: 0.3125rem;
            }
        }

        .instruction-desc {
            font-size: 0.75rem;
            color: var(--text-secondary);
            line-height: 1.5;
        }

        @@media(min-width: 40em) {
            .instruction-desc {
                font-size: 0.8125rem;
            }
        }

        /* Auth Badge */
        .auth-badge {
            background: linear-gradient(135deg, rgba(16, 185, 129, 0.15), rgba(5, 150, 105, 0.08));
            border: 0.125rem solid rgba(16, 185, 129, 0.3);
            border-radius: 0.75rem;
            padding: 0.75rem;
            margin-bottom: 0.875rem;
            display: flex;
            align-items: center;
            gap: 0.75rem;
        }

        @@media(min-width: 40em) {
            .auth-badge {
                border-radius: 0.875rem;
                padding: 0.875rem;
                margin-bottom: 1rem;
                gap: 0.875rem;
            }
        }

        .auth-avatar {
            width: 3rem;
            height: 3rem;
            background: linear-gradient(135deg, var(--success), hsl(142, 71%, 35%));
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 1.25rem;
            flex-shrink: 0;
        }

        @@media(min-width: 40em) {
            .auth-avatar {
                width: 3.25rem;
                height: 3.25rem;
                font-size: 1.375rem;
            }
        }

        .auth-info {
            flex: 1;
            min-width: 0;
        }

        .auth-name {
            font-size: 0.875rem;
            font-weight: 700;
            color: var(--text-primary);
            margin-bottom: 0.25rem;
            overflow: hidden;
            text-overflow: ellipsis;
            white-space: nowrap;
        }

        @@media(min-width: 40em) {
            .auth-name {
                font-size: 0.9375rem;
                margin-bottom: 0.3125rem;
            }
        }

        .auth-status {
            font-size: 0.75rem;
            color: var(--success);
            display: flex;
            align-items: center;
            gap: 0.3125rem;
        }

        @@media(min-width: 40em) {
            .auth-status {
                font-size: 0.8125rem;
            }
        }

        /* Buttons */
        .btn {
            width: 100%;
            padding: 0.875rem 1.125rem;
            border: none;
            border-radius: 0.75rem;
            font-size: 0.9375rem;
            font-weight: 600;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 0.625rem;
            transition: all 0.2s;
            min-height: 3.25rem;
            text-decoration: none;
            touch-action: manipulation;
            -webkit-tap-highlight-color: transparent;
        }

        @@media(min-width: 48em) {
            .btn {
                padding: 1rem 1.25rem;
                font-size: 1rem;
                min-height: 3.5rem;
            }
        }

        .btn:active {
            transform: scale(0.98);
        }

        .btn i {
            font-size: 1.125rem;
            flex-shrink: 0;
        }

        @@media(min-width: 40em) {
            .btn i {
                font-size: 1.25rem;
            }
        }

        .btn-primary {
            background: var(--gradient-primary);
            color: white;
            box-shadow: 0 0.25rem 0.75rem rgba(102, 126, 234, 0.4);
        }

        .btn-secondary {
            background: var(--bg-tertiary);
            color: var(--text-primary);
            border: 0.125rem solid var(--border-color);
            box-shadow: var(--shadow-sm);
        }

        .btn-success {
            background: linear-gradient(135deg, var(--success), hsl(142, 71%, 35%));
            color: white;
            box-shadow: 0 0.25rem 0.75rem hsla(142, 71%, 45%, 0.4);
        }

        .btn:disabled {
            opacity: 0.5;
            cursor: not-allowed;
        }

        /* Form */
        .form-group {
            margin-bottom: 0.875rem;
        }

        @@media(min-width: 48em) {
            .form-group {
                margin-bottom: 1rem;
            }
        }

        .form-label {
            display: flex;
            font-size: 0.875rem;
            font-weight: 600;
            color: var(--text-primary);
            margin-bottom: 0.5rem;
            align-items: center;
            gap: 0.5rem;
        }

        @@media(min-width: 48em) {
            .form-label {
                font-size: 0.9375rem;
                margin-bottom: 0.625rem;
            }
        }

        .form-input {
            width: 100%;
            padding: 0.875rem;
            border: 0.125rem solid var(--border-color);
            border-radius: 0.75rem;
            font-size: 1rem;
            background: var(--bg-primary);
            color: var(--text-primary);
            transition: all 0.2s;
        }

        @@media(min-width: 48em) {
            .form-input {
                padding: 1rem;
                font-size: 1.0625rem;
            }
        }

        .form-input:focus {
            outline: none;
            border-color: var(--accent-color);
            box-shadow: 0 0 0 0.25rem var(--focus-ring);
        }

        /* Messages */
        .message {
            padding: 1rem;
            border-radius: 0.875rem;
            margin-top: 1rem;
            display: flex;
            align-items: flex-start;
            gap: 0.75rem;
            animation: slideIn 0.3s ease-out;
            border: 0.125rem solid;
        }

        .message i {
            font-size: 1.375rem;
            margin-top: 0.125rem;
            flex-shrink: 0;
        }

        .message-text {
            flex: 1;
            font-size: 0.9375rem;
            line-height: 1.5;
        }

        .message-text small {
            display: block;
            margin-top: 0.5rem;
            opacity: 0.8;
            font-size: 0.8125rem;
        }

        .message-success {
            background: hsla(142, 71%, 45%, 0.1);
            border-color: hsla(142, 71%, 45%, 0.4);
            color: var(--success);
        }

        .message-error {
            background: hsla(0, 84%, 60%, 0.1);
            border-color: hsla(0, 84%, 60%, 0.4);
            color: var(--danger);
        }

        @@keyframes slideIn {
            from { opacity: 0; transform: translateY(-0.625rem); }
            to { opacity: 1; transform: translateY(0); }
        }

        @@keyframes slideOut {
            from { opacity: 1; transform: translateY(0); }
            to { opacity: 0; transform: translateY(-0.625rem); }
        }

        /* Estilos adicionales de mensajes */
        .message-loading {
            background: hsla(204, 94%, 60%, 0.1);
            border-color: hsla(204, 94%, 60%, 0.4);
            color: var(--info);
        }

        .message-warning {
            background: hsla(38, 92%, 50%, 0.1);
            border-color: hsla(38, 92%, 50%, 0.4);
            color: var(--warning);
        }

        .message-info {
            background: hsla(204, 94%, 60%, 0.1);
            border-color: hsla(204, 94%, 60%, 0.4);
            color: var(--info);
        }

        [data-theme="dark"] .message-loading,
        [data-theme="dark"] .message-info {
            color: hsl(204, 94%, 70%);
        }

        [data-theme="dark"] .message-warning {
            color: hsl(38, 92%, 60%);
        }

        [data-theme="dark"] .message-success {
            background: hsla(142, 71%, 45%, 0.15);
            border-color: hsla(142, 71%, 45%, 0.5);
            color: hsl(142, 71%, 55%);
        }

        [data-theme="dark"] .message-error {
            background: hsla(0, 84%, 60%, 0.15);
            border-color: hsla(0, 84%, 60%, 0.5);
            color: hsl(0, 84%, 70%);
        }

        /* Camera Modal - Mantiene fixed porque es overlay full-screen */
        .camera-modal {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: hsl(0, 0%, 0%);
            z-index: 1000;
            flex-direction: column;
        }

        .camera-modal.active {
            display: flex;
        }

        .camera-header {
            padding: 1rem;
            background: rgba(0,0,0,0.9);
            display: flex;
            align-items: center;
            justify-content: space-between;
            color: white;
        }

        .camera-title {
            font-size: 1.125rem;
            font-weight: 600;
            display: flex;
            align-items: center;
            gap: 0.625rem;
        }

        .close-camera {
            width: 2.75rem;
            height: 2.75rem;
            border-radius: 50%;
            background: rgba(255,255,255,0.2);
            border: none;
            color: white;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            font-size: 1.375rem;
        }

        .camera-viewport {
            flex: 1;
            position: relative;
        }

        #videoElement {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        .scan-overlay {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            width: 16.25rem;
            height: 16.25rem;
            border: 0.1875rem solid var(--success);
            border-radius: 1.5rem;
            box-shadow: 0 0 0 62.4375rem rgba(0,0,0,0.7);
            animation: scanPulse 2s infinite;
        }

        .scan-overlay.scanning {
            border-color: var(--warning);
        }

        @@keyframes scanPulse {
            0%, 100% { opacity: 1; }
            50% { opacity: 0.7; }
        }

        .scan-hint {
            position: absolute;
            bottom: 2.5rem;
            left: 50%;
            transform: translateX(-50%);
            background: rgba(0,0,0,0.8);
            color: white;
            padding: 0.875rem 1.5rem;
            border-radius: 1.5rem;
            font-size: 0.9375rem;
            max-width: 90%;
            text-align: center;
        }

        /* Responsive */
        @@media(min-width: 40em) {
            .guest-buttons {
                flex-direction: row;
            }

            .btn {
                min-width: 0;
            }
        }

        /* iOS Safe Areas */
        @@supports (-webkit-touch-callout: none) {
            .header {
                padding-top: max(0.75rem, env(safe-area-inset-top));
            }

            .content {
                padding-bottom: max(1rem, env(safe-area-inset-bottom));
            }

            .card,
            .guest-card {
                backdrop-filter: blur(0.9375rem) !important;
                -webkit-backdrop-filter: blur(0.9375rem) !important;
            }
        }
    </style>
}

<!-- Scan Page Content -->
<div class="scan-page">
    <!-- Class Info Badge -->
    <div class="class-info-badge">
        <div class="class-info-icon">
            <i class="fas fa-qrcode"></i>
        </div>
        <div class="class-info-text">
            <div class="class-info-title" id="claseNombre">Cargando clase...</div>
            <div class="class-info-name" id="claseHorario">Registro de Asistencia</div>
        </div>
        <div class="class-id-badge">#@ViewBag.ClaseId</div>
    </div>

    <!-- Instrucciones - Siempre visible -->
    <div class="instructions-card">
        <div class="instructions-header">
            <i class="fas fa-question-circle"></i>
            <span>¿Cómo registrar tu asistencia?</span>
        </div>
        <div class="instructions-list">
            @if (User.Identity?.IsAuthenticated == true)
            {
                <div class="instruction-item">
                    <div class="instruction-number">1</div>
                    <div class="instruction-content">
                        <div class="instruction-title">Registro Rápido (Recomendado)</div>
                        <div class="instruction-desc">Usa el botón "Registrar Mi Asistencia Ahora" para un registro instantáneo con tu cuenta.</div>
                    </div>
                </div>
            }
            else
            {
                <div class="instruction-item">
                    <div class="instruction-number">
                        <i class="fas fa-lightbulb"></i>
                    </div>
                    <div class="instruction-content">
                        <div class="instruction-title">Tip: Crea una cuenta</div>
                        <div class="instruction-desc">Con una cuenta puedes registrar tu asistencia con un solo clic. Usa el botón de menú para ingresar o registrarte.</div>
                    </div>
                </div>
            }
            <div class="instruction-item">
                <div class="instruction-number">
                    @if (User.Identity?.IsAuthenticated == true) { <text>2</text> } else { <text>1</text> }
                </div>
                <div class="instruction-content">
                    <div class="instruction-title">Escanear tu QR de Estudiante</div>
                    <div class="instruction-desc">Abre la cámara y escanea TU código QR de estudiante (formato EST123456).</div>
                </div>
            </div>
            <div class="instruction-item">
                <div class="instruction-number">
                    @if (User.Identity?.IsAuthenticated == true) { <text>3</text> } else { <text>2</text> }
                </div>
                <div class="instruction-content">
                    <div class="instruction-title">Registro Manual por Código</div>
                    <div class="instruction-desc">Ingresa tu código de estudiante (EST123456) manualmente en el formulario de abajo.</div>
                </div>
            </div>
        </div>
    </div>

    @if (User.Identity?.IsAuthenticated == true)
    {
        <!-- Usuario autenticado -->
        <div class="auth-badge">
            <div class="auth-avatar">
                <i class="fas fa-user-check"></i>
            </div>
            <div class="auth-info">
                <div class="auth-name">@User.Identity.Name</div>
                <div class="auth-status">
                    <i class="fas fa-check-circle"></i>
                    Registro automático disponible
                </div>
            </div>
        </div>

        <!-- Registro Automático -->
        <div class="card">
            <div class="card-header">
                <div class="card-icon">
                    <i class="fas fa-bolt"></i>
                </div>
                <div class="card-title">Registro Rápido</div>
            </div>
            <div class="card-desc">
                <i class="fas fa-info-circle"></i> Registra tu asistencia con un solo clic usando tu cuenta
            </div>
            <button class="btn btn-success" id="autoRegisterBtn">
                <i class="fas fa-fingerprint"></i>
                <span>Registrar Mi Asistencia Ahora</span>
            </button>
        </div>
    }

    <!-- Escanear QR -->
    <div class="card">
        <div class="card-header">
            <div class="card-icon">
                <i class="fas fa-camera"></i>
            </div>
            <div class="card-title">Escanear Código QR</div>
        </div>
        <div class="card-desc">
            <i class="fas fa-info-circle"></i> Abre la cámara y escanea TU código QR de estudiante para registrar tu asistencia
        </div>
        <button class="btn btn-primary" id="openCameraBtn">
            <i class="fas fa-qrcode"></i>
            <span>Abrir Cámara</span>
        </button>
    </div>

    <!-- Registro Manual -->
    <div class="card">
        <div class="card-header">
            <div class="card-icon">
                <i class="fas fa-keyboard"></i>
            </div>
            <div class="card-title">Registro Manual por Código</div>
        </div>
        <div class="card-desc">
            <i class="fas fa-info-circle"></i> Ingresa tu código de estudiante (EST123456) para registrar tu asistencia manualmente
        </div>
        <form id="manualForm">
            <div class="form-group">
                <label class="form-label" for="studentId">
                    <i class="fas fa-id-card"></i>
                    Código de Estudiante
                </label>
                <input
                    type="text"
                    id="studentId"
                    class="form-input"
                    placeholder="EST123456"
                    required
                    maxlength="9"
                    pattern="[Ee][Ss][Tt][0-9]{6}"
                    style="text-transform: uppercase;">
            </div>
            <button type="submit" class="btn btn-primary">
                <i class="fas fa-check-circle"></i>
                <span>Registrar Asistencia</span>
            </button>
        </form>
    </div>

    <!-- Messages -->
    <div id="messageArea"></div>
</div>

<!-- Camera Modal -->
<div class="camera-modal" id="cameraModal">
    <div class="camera-header">
        <div class="camera-title">
            <i class="fas fa-qrcode"></i>
            Escanear QR
        </div>
        <button class="close-camera" id="closeCameraBtn">
            <i class="fas fa-times"></i>
        </button>
    </div>
    <div class="camera-viewport">
        <video id="videoElement" playsinline autoplay></video>
        <div class="scan-overlay" id="scanOverlay"></div>
        <div class="scan-hint">
            <i class="fas fa-arrow-down"></i> Centra el código QR en el recuadro
        </div>
    </div>
</div>

@section Scripts {
    <script src="https://unpkg.com/qr-scanner@1.4.2/qr-scanner.umd.min.js"></script>
    <script>
        const claseId = @ViewBag.ClaseId;
        let currentNonce = "@ViewBag.Nonce";
        let qrScanner = null;
        let isProcessing = false;

        // Persistir en sessionStorage para que sobreviva a recargas de página
        const CACHE_KEY = `asistencia_registrada_${claseId}_${currentNonce}`;

        // Recuperar último código registrado (si existe después de recargar)
        let lastRegisteredCode = sessionStorage.getItem(CACHE_KEY);

        // Verificar si ya se registró en esta sesión
        function yaSeRegistro(codigo) {
            return lastRegisteredCode === codigo;
        }

        // Guardar registro en cache persistente
        function guardarEnCache(codigo) {
            sessionStorage.setItem(CACHE_KEY, codigo);
            lastRegisteredCode = codigo;
        }

        // Helper para iconos de estado
        function getEstadoIcon(estado) {
            const icons = {
                'Presente': '<i class="fas fa-check-circle icon-presente"></i>',
                'Tardanza': '<i class="fas fa-clock icon-tardanza"></i>',
                'Ausente': '<i class="fas fa-times-circle icon-ausente"></i>',
                'Justificado': '<i class="fas fa-file-medical icon-justificado"></i>'
            };
            return icons[estado] || '<i class="fas fa-check-circle icon-success"></i>';
        }

        // Cargar info clase
        async function loadClassInfo() {
            try {
                // Usar endpoint público que no requiere autenticación
                const res = await fetch(`/api/clases/public/${claseId}`);
                if (res.ok) {
                    const data = await res.json();
                    // Construir nombre completo: Nombre de Clase - Ramo (Curso)
                    let name = '';

                    if (data.asignatura) {
                        name = data.asignatura;
                    }

                    // Agregar información del ramo si existe
                    if (data.ramo && data.ramo.nombre) {
                        if (name) name += ' - ';
                        name += data.ramo.nombre;

                        // Agregar curso si existe
                        if (data.ramo.curso && data.ramo.curso.nombre) {
                            name += ` (${data.ramo.curso.nombre})`;
                        }
                    }

                    // Fallback si no hay información
                    if (!name) {
                        name = `Clase ${claseId}`;
                    }

                    document.getElementById('claseNombre').textContent = name;

                    // Mostrar horario de la clase
                    if (data.inicioUtc) {
                        const inicio = new Date(data.inicioUtc);
                        const horaInicio = inicio.toLocaleTimeString('es-CL', {
                            hour: '2-digit',
                            minute: '2-digit',
                            hour12: false
                        });

                        let horarioText = `<i class="fas fa-clock"></i> ${horaInicio}`;

                        if (data.finUtc) {
                            const fin = new Date(data.finUtc);
                            const horaFin = fin.toLocaleTimeString('es-CL', {
                                hour: '2-digit',
                                minute: '2-digit',
                                hour12: false
                            });
                            horarioText += ` - ${horaFin}`;

                            // Verificar si la clase está activa o ya terminó
                            const ahora = new Date();
                            if (ahora < fin) {
                                horarioText += ' <span style="color: var(--success); font-weight: 600;">(Activa)</span>';
                            } else {
                                horarioText += ' <span style="color: var(--text-muted); font-weight: 600;">(Finalizada)</span>';
                            }
                        } else {
                            horarioText += ' - <span style="color: var(--success); font-weight: 600;">En curso</span>';
                        }

                        document.getElementById('claseHorario').innerHTML = horarioText;
                    }
                }
            } catch (e) {
                document.getElementById('claseNombre').textContent = `Clase ${claseId}`;
                document.getElementById('claseHorario').textContent = 'Registro de Asistencia';
            }
        }

        // Mostrar mensaje con animación
        function showMessage(text, type = 'error', autoHide = true, duration = null) {
            const area = document.getElementById('messageArea');
            const icons = {
                success: 'check-circle',
                error: 'exclamation-circle',
                warning: 'exclamation-triangle',
                info: 'info-circle',
                loading: 'spinner fa-spin'
            };

            // Duración por defecto según tipo
            if (!duration) {
                duration = type === 'success' ? 8000 : // Éxito: 8 segundos
                          type === 'error' ? 6000 :   // Error: 6 segundos
                          type === 'warning' ? 7000 : // Warning: 7 segundos
                          5000;                       // Otros: 5 segundos
            }

            area.innerHTML = `
                <div class="message message-${type}" style="animation: slideIn 0.3s ease-out;">
                    <i class="fas fa-${icons[type] || icons.info}"></i>
                    <div class="message-text">${text}</div>
                </div>
            `;

            // Scroll suave al mensaje
            area.scrollIntoView({ behavior: 'smooth', block: 'nearest' });

            // Auto-ocultar después del tiempo especificado (excepto loading)
            if (autoHide && type !== 'loading') {
                setTimeout(() => {
                    const msg = area.querySelector('.message');
                    if (msg) {
                        msg.style.animation = 'slideOut 0.3s ease-out';
                        setTimeout(() => area.innerHTML = '', 300);
                    }
                }, duration);
            }
        }

        // Limpiar mensajes
        function clearMessages() {
            document.getElementById('messageArea').innerHTML = '';
        }

        // Registro manual con optimizaciones
        document.getElementById('manualForm').addEventListener('submit', async (e) => {
            e.preventDefault();

            // Evitar doble submit
            if (isProcessing) {
                showMessage('<i class="fas fa-hourglass-half icon-warning"></i> Procesando... por favor espera', 'warning');
                return;
            }

            const codigoAlumno = document.getElementById('studentId').value.trim().toUpperCase();

            // Validar formato EST123456
            if (!/^EST\d{6}$/.test(codigoAlumno)) {
                showMessage('[ERROR] Formato inválido. Debe ser EST seguido de 6 dígitos (Ej: EST123456)', 'error');
                return;
            }

            // Verificar si YA se usó este dispositivo/enlace para registrar
            if (lastRegisteredCode) {
                // Ya se registró alguien desde este dispositivo
                if (lastRegisteredCode === codigoAlumno) {
                    showMessage(
                        `<i class="fas fa-check-circle icon-success"></i> <strong>Ya registraste tu asistencia</strong><br>` +
                        `Código: ${codigoAlumno}<br>` +
                        `<small>Tu asistencia ya está guardada en el sistema.</small>`,
                        'success'
                    );
                    return;
                } else {
                    // Intentando registrar un código diferente
                    // Verificar si el nuevo código ya está registrado en la clase
                    showMessage('<i class="fas fa-spinner fa-spin"></i> Verificando estado del estudiante...', 'loading', false);

                    try {
                        const checkRes = await fetch('/api/asistencias/alumno-scan-codigo', {
                            method: 'POST',
                            headers: { 'Content-Type': 'application/json' },
                            body: JSON.stringify({
                                codigoAlumno: codigoAlumno,
                                claseId,
                                nonce: currentNonce
                            })
                        });

                        const checkData = await checkRes.json();

                        if (checkData.yaRegistrado) {
                            showMessage(
                                `<i class="fas fa-info-circle icon-info"></i> <strong>Este estudiante ya registró su asistencia</strong><br>` +
                                `Código: ${codigoAlumno}<br>` +
                                `<small><i class="fas fa-lightbulb"></i> Este enlace ya se usó para registrar a: ${lastRegisteredCode}</small>`,
                                'info'
                            );
                        } else {
                            showMessage(
                                `<i class="fas fa-ban icon-warning"></i> <strong>Ya se registró una asistencia desde este enlace</strong><br>` +
                                `Ya registrado: <strong>${lastRegisteredCode}</strong><br>` +
                                `Intentas registrar: <strong>${codigoAlumno}</strong> (aún no registrado)<br>` +
                                `<small><i class="fas fa-lightbulb"></i> <strong>Este estudiante NO está registrado aún.</strong> Pide al docente que escanee su QR físico para registrarlo.</small>`,
                                'warning'
                            );
                        }
                    } catch (error) {
                        showMessage(
                            `<i class="fas fa-ban icon-warning"></i> <strong>Ya se registró una asistencia desde este enlace</strong><br>` +
                            `Ya registrado: <strong>${lastRegisteredCode}</strong><br>` +
                            `Intentas registrar: <strong>${codigoAlumno}</strong><br>` +
                            `<small><i class="fas fa-lightbulb"></i> Si este estudiante no está registrado, pide al docente que escanee su QR físico.</small>`,
                            'warning'
                        );
                    }
                    return;
                }
            }

            isProcessing = true;
            const submitBtn = e.target.querySelector('button[type="submit"]');
            const originalBtnText = submitBtn.innerHTML;
            submitBtn.disabled = true;
            submitBtn.innerHTML = '<i class="fas fa-spinner fa-spin"></i><span>Registrando...</span>';

            // Mostrar mensaje de loading
            showMessage('<i class="fas fa-spinner fa-spin"></i> Registrando tu asistencia...', 'loading', false);

            try {
                const startTime = Date.now();

                const res = await fetch('/api/asistencias/alumno-scan-codigo', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({
                        codigoAlumno: codigoAlumno,
                        claseId,
                        nonce: currentNonce
                    })
                });

                const data = await res.json();
                const elapsed = Date.now() - startTime;

                // Mínimo de 500ms para que se vea el feedback
                if (elapsed < 500) {
                    await new Promise(resolve => setTimeout(resolve, 500 - elapsed));
                }

                if (res.ok) {
                    // Guardar en cache
                    guardarEnCache(codigoAlumno);

                    const estado = data.estudiante?.estado || 'Registrado';
                    const nombre = data.estudiante?.nombre || '';
                    const icon = getEstadoIcon(estado);

                    showMessage(
                        `${icon} <strong>¡Asistencia registrada!</strong><br>` +
                        `${nombre ? `Estudiante: ${nombre}<br>` : ''}` +
                        `Código: ${codigoAlumno}<br>` +
                        `Estado: <strong>${estado}</strong>`,
                        'success'
                    );

                    document.getElementById('manualForm').reset();
                } else {
                    throw new Error(data.message || data.mensaje || 'Error al registrar asistencia');
                }
            } catch (error) {
                let msg = error.message;
                let type = 'error';

                // Mensajes optimizados y específicos
                if (msg.includes('ya ha sido registrada') || msg.includes('ya registrada') || msg.includes('ya fue registrada')) {
                    msg = '<i class="fas fa-check-circle icon-success"></i> <strong>Tu asistencia ya fue registrada anteriormente</strong><br>' +
                          `Código: ${codigoAlumno}`;
                    type = 'success';
                    guardarEnCache(codigoAlumno); // Guardar en cache también
                } else if (msg.includes('no encontrado') || msg.includes('no existe')) {
                    msg = `<i class="fas fa-times-circle icon-error"></i> <strong>Código no encontrado</strong><br>` +
                          `El código ${codigoAlumno} no existe en el sistema.<br>` +
                          `<small>Verifica que sea correcto (formato: EST123456)</small>`;
                } else if (msg.includes('no está inscrito') || msg.includes('no estás inscrito') || msg.includes('inscritos pueden registrar')) {
                    msg = '<i class="fas fa-user-slash icon-warning"></i> <strong>No inscrito en el curso</strong><br>' +
                          `El estudiante no está inscrito en el curso de esta clase.<br>` +
                          `<small><i class="fas fa-info-circle"></i> Contacta al profesor para que te inscriba en el curso correspondiente.</small>`;
                    type = 'warning';
                } else if (msg.includes('no activa') || msg.includes('no está activa') || msg.includes('cerrada')) {
                    msg = '<i class="fas fa-exclamation-triangle icon-warning"></i> <strong>Clase no activa</strong><br>' +
                          'Esta clase ya fue cerrada. No se pueden registrar más asistencias.';
                    type = 'warning';
                } else if (msg.includes('expirado') || msg.includes('vencido') || msg.includes('inválido')) {
                    msg = '<i class="fas fa-hourglass-end icon-warning"></i> <strong>Código QR expirado</strong><br>' +
                          'El enlace ha expirado. Solicita un nuevo código QR al profesor.';
                    type = 'warning';
                } else if (msg.toLowerCase().includes('error al registrar') || msg.toLowerCase().includes('no se pudo')) {
                    // Error genérico cuando ya hay un registro previo
                    if (lastRegisteredCode) {
                        if (lastRegisteredCode === codigoAlumno) {
                            msg = '<i class="fas fa-check-circle icon-success"></i> <strong>Ya registraste tu asistencia anteriormente</strong><br>' +
                                  `Código: ${codigoAlumno}<br>` +
                                  '<small><i class="fas fa-info-circle"></i> Tu asistencia ya está guardada en el sistema. No necesitas hacer nada más.</small>';
                            type = 'success';
                        } else {
                            msg = '<i class="fas fa-info-circle icon-info"></i> <strong>Este enlace ya fue utilizado</strong><br>' +
                                  `Ya registrado: <strong>${lastRegisteredCode}</strong><br>` +
                                  `Código ingresado: <strong>${codigoAlumno}</strong><br>` +
                                  '<small><i class="fas fa-lightbulb"></i> <strong>Importante:</strong> Cada enlace QR solo puede registrar UN estudiante. Si <strong>${codigoAlumno}</strong> necesita registrarse, debe usar su propio enlace QR o pedirle al docente que escanee su QR físico de estudiante.</small>';
                            type = 'info';
                        }
                    } else {
                        msg = `<i class="fas fa-exclamation-triangle icon-warning"></i> <strong>No se pudo completar el registro</strong><br>` +
                              `Razón: ${msg}<br>` +
                              `<small><i class="fas fa-redo"></i> Intenta nuevamente o contacta al profesor si el problema persiste.</small>`;
                        type = 'warning';
                    }
                } else {
                    msg = `<i class="fas fa-exclamation-triangle icon-warning"></i> <strong>Error inesperado</strong><br>` +
                          `${msg}<br>` +
                          `<small><i class="fas fa-redo"></i> Por favor intenta nuevamente. Si el error continúa, contacta al profesor.</small>`;
                    type = 'warning';
                }

                showMessage(msg, type);
            } finally {
                isProcessing = false;
                submitBtn.disabled = false;
                submitBtn.innerHTML = originalBtnText;
            }
        });

        // Registro automático con optimizaciones
        document.getElementById('autoRegisterBtn')?.addEventListener('click', async function() {
            if (isProcessing) {
                showMessage('<i class="fas fa-spinner fa-spin"></i> Procesando... por favor espera', 'warning');
                return;
            }

            // Verificar cache (usando username como código)
            const username = '@User.Identity?.Name';
            if (yaSeRegistro(username)) {
                showMessage(`[OK] Ya registraste tu asistencia en esta sesión`, 'success');
                return;
            }

            isProcessing = true;
            const btn = this;
            const originalText = btn.innerHTML;
            btn.disabled = true;
            btn.innerHTML = '<i class="fas fa-spinner fa-spin"></i><span>Registrando...</span>';

            showMessage('<i class="fas fa-spinner fa-spin"></i> Registrando tu asistencia automáticamente...', 'loading', false);

            try {
                const startTime = Date.now();

                const res = await fetch('/api/asistencias/alumno-scan-auto', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ claseId, nonce: currentNonce })
                });

                const data = await res.json();
                const elapsed = Date.now() - startTime;

                if (elapsed < 500) {
                    await new Promise(resolve => setTimeout(resolve, 500 - elapsed));
                }

                if (res.ok) {
                    guardarEnCache(username);

                    const estado = data.estudiante?.estado || 'Registrado';
                    const icon = estado === 'Presente' ? '[OK]' :
                                 estado === 'Tardanza' ? '[TIME]' : '[ERROR]';

                    showMessage(
                        `${icon} <strong>¡Asistencia registrada automáticamente!</strong><br>` +
                        `Usuario: ${username}<br>` +
                        `Estado: <strong>${estado}</strong>`,
                        'success'
                    );
                } else {
                    throw new Error(data.mensaje || 'Error al registrar');
                }
            } catch (error) {
                let msg = error.message;
                let type = 'error';

                if (msg.includes('ya ha sido registrada') || msg.includes('ya registrada')) {
                    msg = '<i class="fas fa-check-circle icon-success"></i> <strong>Ya registraste tu asistencia anteriormente</strong><br>' +
                          `Usuario: ${username}<br>` +
                          '<small><i class="fas fa-info-circle"></i> Tu asistencia ya está guardada en el sistema.</small>';
                    type = 'success';
                    guardarEnCache(username);
                } else if (msg.includes('no está inscrito') || msg.includes('no estás inscrito') || msg.includes('inscritos pueden registrar')) {
                    msg = '<i class="fas fa-user-slash icon-warning"></i> <strong>No inscrito en el curso</strong><br>' +
                          'No estás inscrito en el curso de esta clase.<br>' +
                          '<small><i class="fas fa-info-circle"></i> Contacta al profesor para que te inscriba en el curso correspondiente.</small>';
                    type = 'warning';
                } else if (msg.includes('no activa')) {
                    msg = '<i class="fas fa-exclamation-triangle icon-warning"></i> <strong>Clase no activa</strong><br>' +
                          'Esta clase ya fue cerrada. No se pueden registrar más asistencias.';
                    type = 'warning';
                } else if (msg.includes('expirado')) {
                    msg = '<i class="fas fa-hourglass-end icon-warning"></i> <strong>Código QR expirado</strong><br>' +
                          'El enlace ha expirado. Solicita un nuevo código QR al profesor.';
                    type = 'warning';
                } else if (msg.includes('dispositivo') || msg.includes('ya se registró') ||
                          msg.toLowerCase().includes('ya no se puede') ||
                          msg.toLowerCase().includes('personal')) {
                    msg = '<i class="fas fa-info-circle icon-info"></i> <strong>Este enlace ya fue utilizado</strong><br>' +
                          'El registro es personal. Cada enlace solo puede registrar a un estudiante.<br>' +
                          '<small><i class="fas fa-lightbulb"></i> Si necesitas registrarte, usa tu propio enlace QR o pide al docente que escanee tu QR físico de estudiante.</small>';
                    type = 'info';
                } else if (msg.toLowerCase().includes('error al registrar') && lastRegisteredCode) {
                    msg = '<i class="fas fa-info-circle icon-info"></i> <strong>Este enlace ya fue utilizado</strong><br>' +
                          'El registro es personal. Cada enlace solo puede registrar a un estudiante.<br>' +
                          '<small><i class="fas fa-lightbulb"></i> Si necesitas registrarte, usa tu propio enlace QR o pide al docente que escanee tu QR físico de estudiante.</small>';
                    type = 'info';
                } else {
                    msg = `<i class="fas fa-exclamation-triangle icon-warning"></i> <strong>Error inesperado</strong><br>` +
                          `${msg}<br>` +
                          `<small><i class="fas fa-redo"></i> Por favor intenta nuevamente. Si el error continúa, contacta al profesor.</small>`;
                    type = 'warning';
                }

                showMessage(msg, type);
            } finally {
                isProcessing = false;
                btn.disabled = false;
                btn.innerHTML = originalText;
            }
        });

        // Cámara QR
        const modal = document.getElementById('cameraModal');
        const video = document.getElementById('videoElement');
        const overlay = document.getElementById('scanOverlay');

        document.getElementById('openCameraBtn').addEventListener('click', async () => {
            try {
                const stream = await navigator.mediaDevices.getUserMedia({
                    video: { facingMode: 'environment' }
                });
                video.srcObject = stream;
                modal.classList.add('active');
                startScanning();
            } catch (error) {
                showMessage('[ERROR] No se pudo acceder a la cámara. Verifica los permisos.', 'error');
            }
        });

        document.getElementById('closeCameraBtn').addEventListener('click', () => {
            closeCamera();
        });

        function closeCamera() {
            if (qrScanner) {
                qrScanner.stop();
                qrScanner = null;
            }
            if (video.srcObject) {
                video.srcObject.getTracks().forEach(track => track.stop());
                video.srcObject = null;
            }
            modal.classList.remove('active');
        }

        function startScanning() {
            overlay.classList.add('scanning');
            qrScanner = new QrScanner(
                video,
                result => handleQRCode(result.data),
                {
                    onDecodeError: () => {},
                    highlightScanRegion: false,
                    highlightCodeOutline: false,
                    maxScansPerSecond: 5
                }
            );
            qrScanner.start();
        }

        // Extraer código de estudiante del QR
        function extractCodigo(qrData) {
            // Formato directo EST123456
            if (/^EST\d{6}$/i.test(qrData)) return qrData.toUpperCase();

            // Formato JSON
            try {
                if (qrData.startsWith('{')) {
                    const data = JSON.parse(qrData);
                    return data.studentCode || data.codigo || data.code;
                }
            } catch (e) {}

            // Buscar patrón EST123456 en el texto
            const match = qrData.match(/EST\d{6}/i);
            return match ? match[0].toUpperCase() : null;
        }

        async function handleQRCode(data) {
            // Evitar procesar si ya está procesando
            if (isProcessing) {
                return;
            }

            overlay.classList.remove('scanning');
            closeCamera();

            // Extraer código de estudiante del QR escaneado
            const codigoAlumno = extractCodigo(data);

            if (!codigoAlumno) {
                showMessage('[WARNING] Código QR no válido. Asegúrate de escanear tu QR de estudiante (formato EST123456)', 'warning');
                return;
            }

            // Verificar si YA se usó este dispositivo/enlace para registrar
            if (lastRegisteredCode) {
                // Ya se registró alguien desde este dispositivo
                if (lastRegisteredCode === codigoAlumno) {
                    showMessage(
                        `<i class="fas fa-check-circle icon-success"></i> <strong>Ya registraste tu asistencia</strong><br>` +
                        `Código: ${codigoAlumno}<br>` +
                        `<small>Tu asistencia ya está guardada en el sistema.</small>`,
                        'success'
                    );
                    return;
                } else {
                    // Intentando registrar un código diferente
                    showMessage('<i class="fas fa-spinner fa-spin"></i> Verificando estado del estudiante...', 'loading', false);

                    try {
                        const checkRes = await fetch('/api/asistencias/alumno-scan-codigo', {
                            method: 'POST',
                            headers: { 'Content-Type': 'application/json' },
                            body: JSON.stringify({
                                codigoAlumno: codigoAlumno,
                                claseId,
                                nonce: currentNonce
                            })
                        });

                        const checkData = await checkRes.json();

                        if (checkData.yaRegistrado) {
                            showMessage(
                                `<i class="fas fa-info-circle icon-info"></i> <strong>Este estudiante ya registró su asistencia</strong><br>` +
                                `Código: ${codigoAlumno}<br>` +
                                `<small><i class="fas fa-lightbulb"></i> Este enlace ya se usó para registrar a: ${lastRegisteredCode}</small>`,
                                'info'
                            );
                        } else {
                            showMessage(
                                `<i class="fas fa-ban icon-warning"></i> <strong>Ya se registró una asistencia desde este enlace</strong><br>` +
                                `Ya registrado: <strong>${lastRegisteredCode}</strong><br>` +
                                `QR escaneado: <strong>${codigoAlumno}</strong> (aún no registrado)<br>` +
                                `<small><i class="fas fa-lightbulb"></i> <strong>Este estudiante NO está registrado aún.</strong> Pide al docente que escanee su QR físico para registrarlo.</small>`,
                                'warning'
                            );
                        }
                    } catch (error) {
                        showMessage(
                            `<i class="fas fa-ban icon-warning"></i> <strong>Ya se registró una asistencia desde este enlace</strong><br>` +
                            `Ya registrado: <strong>${lastRegisteredCode}</strong><br>` +
                            `QR escaneado: <strong>${codigoAlumno}</strong><br>` +
                            `<small><i class="fas fa-lightbulb"></i> Si este estudiante no está registrado, pide al docente que escanee su QR físico.</small>`,
                            'warning'
                        );
                    }
                    return;
                }
            }

            // Llenar el input con el código escaneado
            document.getElementById('studentId').value = codigoAlumno;

            isProcessing = true;

            // Feedback inmediato - UI Optimista
            showMessage(`<i class="fas fa-spinner fa-spin"></i> Registrando ${codigoAlumno}...`, 'loading', false);

            // Registrar asistencia con el código escaneado
            try {
                const startTime = Date.now();

                const res = await fetch('/api/asistencias/alumno-scan-codigo', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({
                        codigoAlumno: codigoAlumno,
                        claseId,
                        nonce: currentNonce
                    })
                });

                const result = await res.json();
                const elapsed = Date.now() - startTime;

                // Mínimo 400ms para feedback visual
                if (elapsed < 400) {
                    await new Promise(resolve => setTimeout(resolve, 400 - elapsed));
                }

                if (res.ok) {
                    // Guardar en cache
                    guardarEnCache(codigoAlumno);

                    const estado = result.estudiante?.estado || 'Registrado';
                    const nombre = result.estudiante?.nombre || '';
                    const icon = estado === 'Presente' ? '[OK]' :
                                 estado === 'Tardanza' ? '[TIME]' : '[ERROR]';

                    showMessage(
                        `${icon} <strong>¡Asistencia registrada por QR!</strong><br>` +
                        `${nombre ? `Estudiante: ${nombre}<br>` : ''}` +
                        `Código: ${codigoAlumno}<br>` +
                        `Estado: <strong>${estado}</strong>`,
                        'success'
                    );

                    // Vibrar para feedback háptico (si está disponible)
                    if (navigator.vibrate) {
                        navigator.vibrate([100, 50, 100]);
                    }
                } else {
                    throw new Error(result.message || result.mensaje || 'Error al registrar');
                }
            } catch (error) {
                let msg = error.message;
                let type = 'error';

                // Mensajes optimizados y específicos
                if (msg.includes('ya ha sido registrada') || msg.includes('ya registrada') || msg.includes('ya fue registrada')) {
                    msg = '<i class="fas fa-check-circle icon-success"></i> <strong>Tu asistencia ya fue registrada</strong><br>' +
                          `Código: ${codigoAlumno}`;
                    type = 'success';
                    guardarEnCache(codigoAlumno);
                } else if (msg.includes('no encontrado') || msg.includes('no existe')) {
                    msg = `<i class="fas fa-times-circle icon-error"></i> <strong>Código no encontrado</strong><br>` +
                          `El código ${codigoAlumno} no existe en el sistema.`;
                } else if (msg.includes('no está inscrito') || msg.includes('no estás inscrito') || msg.includes('inscritos pueden registrar')) {
                    msg = '<i class="fas fa-user-slash icon-warning"></i> <strong>No inscrito en el curso</strong><br>' +
                          `El estudiante no está inscrito en el curso de esta clase.<br>` +
                          `<small><i class="fas fa-info-circle"></i> Contacta al profesor para que te inscriba en el curso correspondiente.</small>`;
                    type = 'warning';
                } else if (msg.includes('no activa') || msg.includes('no está activa') || msg.includes('cerrada')) {
                    msg = '<i class="fas fa-exclamation-triangle icon-warning"></i> <strong>Clase no activa</strong><br>' +
                          'Esta clase ya fue cerrada.';
                    type = 'warning';
                } else if (msg.includes('expirado') || msg.includes('vencido') || msg.includes('inválido')) {
                    msg = '<i class="fas fa-hourglass-end icon-warning"></i> <strong>Código QR expirado</strong><br>' +
                          'Solicita un nuevo código QR al profesor.';
                    type = 'warning';
                } else if (msg.toLowerCase().includes('error al registrar') || msg.toLowerCase().includes('no se pudo')) {
                    // Error genérico cuando ya hay un registro previo
                    if (lastRegisteredCode) {
                        if (lastRegisteredCode === codigoAlumno) {
                            msg = '<i class="fas fa-check-circle icon-success"></i> <strong>Ya registraste tu asistencia anteriormente</strong><br>' +
                                  `Código: ${codigoAlumno}<br>` +
                                  '<small><i class="fas fa-info-circle"></i> Tu asistencia ya está guardada en el sistema. No necesitas hacer nada más.</small>';
                            type = 'success';
                        } else {
                            msg = '<i class="fas fa-info-circle icon-info"></i> <strong>Este enlace ya fue utilizado</strong><br>' +
                                  `Ya registrado: <strong>${lastRegisteredCode}</strong><br>` +
                                  `QR escaneado: <strong>${codigoAlumno}</strong><br>` +
                                  '<small><i class="fas fa-lightbulb"></i> <strong>Importante:</strong> Cada enlace QR solo puede registrar UN estudiante. Si <strong>${codigoAlumno}</strong> necesita registrarse, debe usar su propio enlace QR o pedirle al docente que escanee su QR físico de estudiante.</small>';
                            type = 'info';
                        }
                    } else {
                        msg = `<i class="fas fa-exclamation-triangle icon-warning"></i> <strong>No se pudo completar el registro</strong><br>` +
                              `Razón: ${msg}<br>` +
                              `<small><i class="fas fa-redo"></i> Intenta nuevamente o contacta al profesor si el problema persiste.</small>`;
                        type = 'warning';
                    }
                } else {
                    msg = `<i class="fas fa-exclamation-triangle icon-warning"></i> <strong>Error inesperado</strong><br>` +
                          `${msg}<br>` +
                          `<small><i class="fas fa-redo"></i> Por favor intenta nuevamente. Si el error continúa, contacta al profesor.</small>`;
                    type = 'warning';
                }

                showMessage(msg, type);

                // Vibrar error
                if (navigator.vibrate && type === 'error') {
                    navigator.vibrate(200);
                }
            } finally {
                isProcessing = false;
            }
        }

        // Validación en tiempo real del input
        const studentIdInput = document.getElementById('studentId');
        studentIdInput?.addEventListener('input', (e) => {
            let value = e.target.value.toUpperCase().replace(/[^A-Z0-9]/g, '');
            e.target.value = value;

            // Validar formato completo EST123456
            const valid = /^EST\d{6}$/.test(value);
            const partial = /^(E|ES|EST|EST\d{1,5})$/.test(value);

            e.target.classList.remove('valid', 'invalid');
            if (valid) {
                e.target.style.borderColor = 'hsl(142, 71%, 45%)';
                e.target.style.boxShadow = '0 0 0 0.25rem hsla(142, 71%, 45%, 0.15)';
            } else if (value && !partial) {
                e.target.style.borderColor = 'hsl(0, 84%, 60%)';
                e.target.style.boxShadow = '0 0 0 0.25rem hsla(0, 84%, 60%, 0.15)';
            } else {
                e.target.style.borderColor = '';
                e.target.style.boxShadow = '';
            }
        });

        // Función para inicializar la página al cargar/refrescar
        function initPage() {
            // IMPORTANTE: Limpiar cualquier mensaje previo del DOM
            clearMessages();

            // Verificar si ya se registró una asistencia en esta sesión (enlace)
            if (lastRegisteredCode) {
                // Mostrar mensaje informativo persistente (NO de error)
                showMessage(
                    `<i class="fas fa-check-circle"></i> <strong>Asistencia ya registrada</strong><br>` +
                    `Código registrado: <strong>${lastRegisteredCode}</strong><br>` +
                    `<small><i class="fas fa-info-circle"></i> Tu asistencia está guardada en el sistema. Si refrescas la página, esta confirmación permanecerá visible.</small>`,
                    'success',
                    false  // No auto-ocultar para que sea persistente
                );
            }

            // Cargar información de la clase
            loadClassInfo();
        }

        // Ejecutar inicialización al cargar la página
        initPage();
    </script>
}
